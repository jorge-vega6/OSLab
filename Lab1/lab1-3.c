#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

#define MAX_CHARS 10000

int main() {

    char cmd[MAX_CHARS];
    int t;
    scanf("%d\n", &t);
    while(t--) {
        fgets(cmd, MAX_CHARS, stdin);	    //cmd has current string 
    
        // Add you code here
    }
    return 0;
}
